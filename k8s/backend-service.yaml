apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: idea-board
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP