apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: idea-board
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: ideas
            - name: POSTGRES_USER
              value: ideas_user
            - name: POSTGRES_PASSWORD
              value: ideas_pass
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: idea-board
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
